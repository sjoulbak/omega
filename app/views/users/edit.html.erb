<h1>Editing User</h1>
<div class="context">
  <%= render 'form' %>

  <%= link_to 'Show', @user %> |
  <% if current_user.admin? %>
    <%= link_to 'Back', users_path %>
  <% else %>
    <%= link_to 'Back', root_path %>
  <% end %>
</div>
